using UnityEngine;

public class practiceCoding : MonoBehaviour
{

    int coins = 3;
    int totalScore = 25*(coins);

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    print (totalScore);        
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
