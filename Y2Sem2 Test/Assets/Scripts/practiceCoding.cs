using UnityEngine;

public class practiceCoding : MonoBehaviour
{

    int coins = 25;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

        total score = 3 * (coins);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
